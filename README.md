Next feature to do -> Save dates with message to non-volatile memory (probably in a database)
Then -> Create a thread that reads dates in batches, loads dates into ram, processes those dates into tokio jobs
